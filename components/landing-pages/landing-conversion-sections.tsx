'use client'

import Image from 'next/image'
import Link from 'next/link'
import { useEffect, useMemo, useState } from 'react'
import type { LandingSection } from '@/lib/landing-pages/types'
import type { StorefrontProduct, StorefrontVariant } from '@/lib/services/storefront-utils'
import { getStartingPrice } from '@/lib/services/storefront-utils'

function money(value: number | null) {
  return value === null ? 'View current price' : `৳${value.toLocaleString('en-BD')}`
}

function productFor(section: LandingSection, products: StorefrontProduct[], fallback: StorefrontProduct | undefined) {
  if (!('productId' in section) || !section.productId) return fallback
  return products.find((product) => product.id === section.productId) || fallback
}

function variantLabel(variant: StorefrontVariant) {
  return [variant.ram, variant.storage, variant.color].filter(Boolean).join(' / ') || variant.variant_title
}

function ProductGallery({ section, product }: { section: Extract<LandingSection, { type: 'product_gallery' }>; product?: StorefrontProduct }) {
  const images = product?.images ?? []
  const [selectedImage, setSelectedImage] = useState('')
  const activeImage = images.some((image) => image.image_url === selectedImage) ? selectedImage : (images[0]?.image_url || '')
  if (!product) return null
  return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_8rem]"><div className="relative aspect-square overflow-hidden rounded-2xl bg-slate-100">{activeImage ? <Image src={activeImage} alt={product.name} fill className="object-contain" sizes="(max-width: 1024px) 100vw, 60vw" /> : null}</div><div className="grid grid-cols-4 gap-3 lg:grid-cols-1">{images.map((image) => <button type="button" key={image.id} onClick={() => setSelectedImage(image.image_url)} aria-label={`View ${image.alt_text || product.name}`} className={`relative aspect-square overflow-hidden rounded-xl border-2 bg-slate-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500 ${activeImage === image.image_url ? 'border-emerald-500' : 'border-transparent'}`}><Image src={image.image_url} alt="" fill className="object-contain" sizes="96px" /></button>)}</div></div>{section.title ? <h2 className="mt-5 text-2xl font-semibold tracking-tight text-slate-950">{section.title}</h2> : null}<p className="mt-2 text-sm leading-6 text-slate-600">{product.name}</p></section>
}

function VariantSelector({ section, product, selectedVariantId, onSelect }: { section: Extract<LandingSection, { type: 'variant_selector' }>; product?: StorefrontProduct; selectedVariantId: string; onSelect: (id: string) => void }) {
  if (!product || !product.variants.length) return null
  return <section aria-labelledby={`${section.id}-title`} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><h2 id={`${section.id}-title`} className="text-2xl font-semibold tracking-tight text-slate-950">{section.title || 'Choose your variant'}</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{product.variants.map((variant) => <button type="button" key={variant.id} onClick={() => onSelect(variant.id)} disabled={!variant.is_in_stock} aria-pressed={selectedVariantId === variant.id} className={`rounded-2xl border p-4 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500 ${selectedVariantId === variant.id ? 'border-emerald-500 bg-emerald-50' : 'border-slate-200 bg-white hover:border-emerald-300'} ${!variant.is_in_stock ? 'cursor-not-allowed opacity-50' : ''}`}><span className="block font-bold text-slate-950">{variantLabel(variant)}</span><span className="mt-1 block text-sm font-black text-emerald-700">{money(variant.price)}</span><span className="mt-1 block text-xs font-semibold text-slate-500">{variant.is_in_stock ? (variant.is_low_stock ? 'Limited current availability' : 'Available now') : 'Currently unavailable'}</span></button>)}</div></section>
}

function QuantitySelector({ section, quantity, onChange }: { section: Extract<LandingSection, { type: 'quantity_selector' }>; quantity: number; onChange: (value: number) => void }) {
  const max = section.maxQuantity ?? 10
  return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-semibold text-slate-950">{section.title || 'Select quantity'}</h2><p className="mt-1 text-sm text-slate-500">Final availability is checked by the server during checkout.</p></div><div className="inline-flex items-center rounded-xl border border-slate-200 bg-slate-50 p-1"><button type="button" onClick={() => onChange(Math.max(1, quantity - 1))} aria-label="Decrease quantity" className="h-10 w-10 rounded-lg text-lg font-black text-slate-700 hover:bg-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500">−</button><span aria-live="polite" className="min-w-10 text-center text-sm font-black text-slate-950">{quantity}</span><button type="button" onClick={() => onChange(Math.min(max, quantity + 1))} aria-label="Increase quantity" className="h-10 w-10 rounded-lg text-lg font-black text-slate-700 hover:bg-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500">+</button></div></div></section>
}

function Countdown({ section }: { section: Extract<LandingSection, { type: 'countdown' }> }) {
  const [remaining, setRemaining] = useState('')
  useEffect(() => { const tick = () => { const ms = new Date(section.endsAt).getTime() - Date.now(); if (ms <= 0) return setRemaining('This campaign has ended'); const totalSeconds = Math.floor(ms / 1000); const days = Math.floor(totalSeconds / 86400); const hours = Math.floor((totalSeconds % 86400) / 3600); const minutes = Math.floor((totalSeconds % 3600) / 60); const seconds = totalSeconds % 60; setRemaining(`${days}d ${hours}h ${minutes}m ${seconds}s`) }; tick(); const timer = window.setInterval(tick, 1000); return () => window.clearInterval(timer) }, [section.endsAt])
  return <section role="timer" aria-live="polite" className="rounded-3xl bg-slate-950 p-6 text-white sm:p-8"><p className="text-xs font-black uppercase tracking-[0.18em] text-emerald-300">Campaign timing</p><h2 className="mt-2 text-2xl font-semibold">{section.title}</h2>{section.body ? <p className="mt-2 text-sm leading-6 text-slate-300">{section.body}</p> : null}<p className="mt-5 text-3xl font-black tracking-tight text-emerald-300">{remaining || 'Calculating…'}</p></section>
}

export function LandingConversionSections({ sections, products }: { sections: LandingSection[]; products: StorefrontProduct[] }) {
  const fallback = products[0]
  const declaredSection = sections.find((section): section is LandingSection & { productId: string } => 'productId' in section && typeof section.productId === 'string')
  const declaredProductId = declaredSection?.productId
  const selectedProductId = declaredProductId || fallback?.id || ''
  const selectedProduct = products.find((product) => product.id === selectedProductId) || fallback
  const [selectedVariantId, setSelectedVariantId] = useState(selectedProduct?.variants.find((variant) => variant.is_in_stock)?.id || selectedProduct?.variants[0]?.id || '')
  const [quantity, setQuantity] = useState(1)
  const selectedVariant = useMemo(() => selectedProduct?.variants.find((variant) => variant.id === selectedVariantId), [selectedProduct, selectedVariantId])
  const orderHref = selectedProduct && selectedVariant ? `/order?productId=${encodeURIComponent(selectedProduct.id)}&variantId=${encodeURIComponent(selectedVariant.id)}&quantity=${quantity}` : '/order'

  return <>{sections.map((section) => { if (section.enabled === false) return null; if (section.type === 'product_gallery') { const product = productFor(section, products, selectedProduct); return <ProductGallery key={section.id} section={section} product={product} /> } if (section.type === 'variant_selector') { const product = productFor(section, products, selectedProduct); return <VariantSelector key={section.id} section={section} product={product} selectedVariantId={selectedVariantId} onSelect={setSelectedVariantId} /> } if (section.type === 'quantity_selector') return <QuantitySelector key={section.id} section={section} quantity={quantity} onChange={setQuantity} />; if (section.type === 'offer') { const product = productFor(section, products, selectedProduct); const price = product ? getStartingPrice(product) : null; return <section key={section.id} className="rounded-3xl border border-emerald-200 bg-emerald-50 p-6 sm:p-8"><p className="text-xs font-black uppercase tracking-[0.18em] text-emerald-700">{section.discountText || 'Current offer'}</p><h2 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">{section.title}</h2>{section.body ? <p className="mt-3 max-w-2xl text-sm leading-6 text-slate-700">{section.body}</p> : null}{section.showLivePrice && price !== null ? <p className="mt-5 text-2xl font-black text-emerald-800">From {money(price)}</p> : null}{section.ctaLabel ? <Link href={section.ctaHref || orderHref} className="mt-6 inline-flex min-h-11 items-center rounded-xl bg-slate-950 px-5 py-3 text-sm font-black text-white hover:bg-emerald-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500">{section.ctaLabel}</Link> : null}</section> } if (section.type === 'countdown') return <Countdown key={section.id} section={section} />; if (section.type === 'delivery_info' || section.type === 'warranty' || section.type === 'social_proof') return <section key={section.id} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><h2 className="text-2xl font-semibold tracking-tight text-slate-950">{section.title}</h2>{'body' in section ? <p className="mt-3 max-w-3xl text-sm leading-7 text-slate-600">{section.body}</p> : null}{'disclaimer' in section && section.disclaimer ? <p className="mt-4 text-xs font-semibold text-slate-500">{section.disclaimer}</p> : null}{'href' in section && section.href ? <Link href={section.href} className="mt-5 inline-flex text-sm font-black text-emerald-700 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500">Learn more</Link> : null}{section.type === 'warranty' ? <ul className="mt-5 grid gap-3 sm:grid-cols-2">{section.items.map((item) => <li key={item} className="rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-slate-800">{item}</li>)}</ul> : null}</section>; if (section.type === 'order') return <section key={section.id} className="rounded-3xl bg-emerald-700 p-6 text-white sm:p-8"><h2 className="text-3xl font-semibold">{section.title || 'Ready to order?'}</h2>{section.body ? <p className="mt-3 max-w-2xl text-sm leading-6 text-emerald-50">{section.body}</p> : null}<p className="mt-4 text-sm text-emerald-100">{selectedProduct?.name || 'Select a product'} · {selectedVariant ? variantLabel(selectedVariant) : 'Choose a variant'} · {quantity} item{quantity === 1 ? '' : 's'}</p><Link href={orderHref} data-analytics-event="landing_page_order_cta" data-landing-section={section.id} className="mt-6 inline-flex min-h-12 items-center rounded-xl bg-white px-6 py-3 text-sm font-black text-emerald-800 hover:bg-emerald-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-emerald-700">{section.ctaLabel || 'Continue to secure checkout'}</Link></section>; if (section.type === 'related_products') { const related = (section.productIds?.map((id) => products.find((product) => product.id === id)).filter(Boolean) as StorefrontProduct[]) || products.slice(0, 3); return <section key={section.id}><h2 className="text-2xl font-semibold tracking-tight text-slate-950">{section.title || 'You may also like'}</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{related.map((product) => <Link key={product.id} href={`/products/${product.slug}`} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:border-emerald-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500"><p className="font-bold text-slate-950">{product.name}</p><p className="mt-2 text-sm font-black text-emerald-700">{money(getStartingPrice(product))}</p></Link>)}</div></section> } if (section.type === 'sticky_mobile_cta') return <div key={section.id} className="fixed inset-x-4 bottom-4 z-20 sm:hidden"><Link href={section.productId && selectedVariant ? orderHref : section.href} className="flex min-h-12 items-center justify-center rounded-full bg-slate-950 px-5 py-3 text-sm font-black text-white shadow-2xl focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-400">{section.label}</Link></div>; return null })}</>
}
